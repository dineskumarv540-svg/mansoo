# Backend Architecture: Firebase Suite

- Firebase Authentication (Email/Password, Google OAuth).
- Cloud Firestore (NoSQL document database).
- Cloud Storage (Media & profile images).
- Cloud Functions (Node.js 20 callable backend validation).
