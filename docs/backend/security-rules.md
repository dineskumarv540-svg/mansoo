# Security Rules Guide

See full rules in `firestore.rules` and `storage.rules`.
- Ensures users can only write to their own resource documents.
- Restricts reports collection read/update to Cloud Admin SDK only.
